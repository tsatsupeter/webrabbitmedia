Update `src/merchant/nav.js` Support group children to:

- `Documentation` → links to `/docs` (public docs page)
- `Feature Request` → no route yet (placeholder)

Remove `Tickets`. No other nav changes.
